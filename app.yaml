build:
  commands:
    build:
      - npm install
      - npm run build
  env:
    NODE_ENV: production

run:
  command: ["npm", "start"]
  env:
    PYTHONPATH: "."
  resources:
    memory: 6G
    cpus: 2
